/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = {
  images: {
    domains: ['images.pexels.com', 's3-alpha-sig.figma.com', 'api-distressales.onrender.com'],
  },
};
